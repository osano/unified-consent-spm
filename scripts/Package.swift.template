// swift-tools-version:5.3
import PackageDescription

let package = Package(
    name: "UnifiedConsentSDK",
    platforms: [
        .iOS(.v13)
    ],
    products: [
        .library(
            name: "UnifiedConsentSDK",
            targets: ["UnifiedConsentSDK"])
    ],
    dependencies: [],
    targets: [
        .binaryTarget(
            name: "UnifiedConsentSDK",
            url: "https://libraries.osano.com/ios/UnifiedConsentSDK/UnifiedConsentSDK-{{{version}}}.zip",
            checksum: "{{{checksum}}}"
        ),
    ]
) 